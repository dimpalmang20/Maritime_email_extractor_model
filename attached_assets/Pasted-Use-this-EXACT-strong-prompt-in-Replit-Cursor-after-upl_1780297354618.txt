Use this EXACT strong prompt in Replit / Cursor after uploading your current GitHub project.

This prompt is focused ONLY on:

* ML + extraction engine
* segmentation
* accuracy
* maritime intelligence
* no frontend
* no vite/react/database

and directly matches what your sir and company want.

CRITICAL ENTERPRISE TASK — BUILD HIGH-ACCURACY MARITIME EXTRACTION MODEL (95–97% TARGET)

You are working on an enterprise maritime email extraction system for shipbroking and chartering operations.

IMPORTANT:
The current project already contains a strong rule-based + template parsing engine.
DO NOT replace or weaken the current logic.

The current parser is already performing well.
Your task is to significantly improve:

* extraction accuracy
* segmentation accuracy
* contextual isolation
* maritime normalization
* multi-order parsing
* dynamic response generation

GOAL:
Achieve near 95–97% extraction accuracy for:

* short emails
* long emails
* multiline broker circulars
* malformed formatting
* forwarded chains
* mixed TC/VC/Tonnage emails
* noisy signatures
* operational maritime clauses

VERY IMPORTANT:
This project must remain:

* standalone Node.js API
* TypeScript only
* NO frontend
* NO React
* NO Vite
* NO database
* NO PostgreSQL
* NO authentication
* NO environment variables
* NO unnecessary setup

Focus ONLY on:

* maritime extraction engine
* parsing logic
* segmentation engine
* maritime dictionaries
* local ML/NLP enhancement
* confidence scoring
* enterprise JSON output

====================================================
PRIMARY PROBLEMS TO SOLVE
=========================

1. MULTI-ORDER EXTRACTION FAILURE

Current issue:
Large broker emails contain multiple:

* TC cargoes
* VC cargoes
* tonnage positions

But parser sometimes:

* merges blocks
* misses blocks
* leaks fields between blocks

REQUIRED:
Implement robust contextual segmentation.

Every independent cargo/tonnage order must produce:
ONE CLEAN ISOLATED JSON OBJECT.

NO BLOCK BLEEDING.

====================================================
2. DYNAMIC RESPONSE SCHEMA
==========================

Current issue:
API returns all fields for all email types.

This is incorrect.

REQUIRED:
Dynamic schema generation.

If email_type = TONNAGE:
return ONLY tonnage fields.

If email_type = CARGO_TC:
return ONLY TC fields.

If email_type = CARGO_VC:
return ONLY VC fields.

Output must be:

* minimal
* clean
* enterprise structured
* type-aware

====================================================
3. SEGMENTATION ENGINE IMPROVEMENT
==================================

Build enterprise-grade segmentation.

Support:

* dashed separators
* ACCT
* A/C
* ACCOUNT
* broker bullet formatting
* repeated signatures
* numbered sections
* whitespace grouping
* broker circular chains
* multiline grouped clauses

Must correctly split:

* mixed email chains
* forwarded brokers
* multiline cargo clauses
* vessel blocks

====================================================
4. PREPROCESSING ENGINE
=======================

Before extraction:

* normalize whitespace
* preserve semantic structure
* remove noisy signatures
* remove mobile footers
* normalize separators
* normalize maritime abbreviations
* preserve line meaning
* remove irrelevant contact blocks

====================================================
5. CARGO EXTRACTION IMPROVEMENT
===============================

Current issue:
Parser incorrectly extracts:

* broker names
* signatures
* ADCOM
* A/C
* company names
  as cargo.

REQUIRED:
Implement strong negative filtering and semantic cleanup.

Cargo extraction must support:

* multiline cargo descriptions
* combined cargoes
* shorthand maritime cargo terms

====================================================
6. DATE PARSING IMPROVEMENT
===========================

Must support:

* SPOT
* PROMPT
* ONWARDS
* END JULY
* MID JULY
* EARLY JULY
* END OF JULY ONWARDS

Implement robust year inference and normalization.

All dates must become:
YYYY-MM-DD

====================================================
7. MARITIME KNOWLEDGE ENGINE
============================

Expand maritime dictionaries:

* ports
* countries
* cargo aliases
* vessel aliases
* chartering abbreviations
* maritime regions

Examples:
ECI = East Coast India
WAFR = West Africa
SMX = Supramax
UMX = Ultramax
BSEA = Black Sea
COGH = Cape of Good Hope

====================================================
8. LIGHTWEIGHT LOCAL ML/NLP LAYER
=================================

Use lightweight local intelligence only.

Allowed:

* spaCy
* local transformers
* DistilBERT
* heuristic classification
* local NLP

NO expensive hosted APIs.

ML should help:

* ambiguous extraction
* entity recovery
* cargo classification
* semantic correction
* confidence improvement

====================================================
9. VALIDATION ENGINE
====================

Implement validation:

* DWT validation
* date validation
* port normalization
* quantity normalization
* vessel-type consistency
* cargo-type consistency

If laycan_end missing:
auto-generate:
laycan_start + 5 days

====================================================
10. CONFIDENCE ENGINE
=====================

Confidence must depend on:

* extraction completeness
* segmentation quality
* regex certainty
* maritime dictionary matching
* validation success
* contextual consistency

====================================================
11. REAL-WORLD TESTING
======================

Test against:

* extremely large broker emails
* multiline chains
* mixed TC/VC/Tonnage emails
* malformed formatting
* repeated broker signatures
* broken spacing
* operational clauses
* noisy forwarding chains

====================================================
12. FINAL REQUIREMENT
=====================

Before finishing:

1. Run extensive extraction tests
2. Verify multi-order extraction works
3. Verify segmentation isolation
4. Verify dynamic JSON schemas
5. Verify no field leakage occurs
6. Verify long broker circulars produce multiple clean outputs
7. Preserve current successful extraction logic
8. Improve accuracy WITHOUT weakening existing parser

FINAL GOAL:
Deliver a production-grade standalone maritime extraction model with near 95–97% real-world extraction accuracy while remaining lightweight, local, fast, and low-cost.
